package model.card.wild;

public class Saver extends Wild {
	Saver(String name, String description, BoardManager boardManager, GameManager gameManager) {
		super(name, description, boardManager, gameManager);
	}
}
